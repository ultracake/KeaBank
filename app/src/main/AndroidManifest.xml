<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.keabank">

    <application
        android:name=".Services.Home"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.Create.CreateBillActivity"></activity>
        <activity android:name=".Activities.List.ListOfBillsActivity" />
        <activity android:name=".Activities.BillActivity" />
        <activity android:name=".Services.TransferServiceActivity" />
        <activity android:name=".Activities.TransferActivity" />
        <activity android:name=".Activities.ViewAccountActivity" />
        <activity android:name=".Activities.ForgotPasswordActivity" />
        <activity android:name=".Activities.List.ListOfAccountsActivity" />
        <activity android:name=".Activities.Create.CreateAccountActivity" />
        <activity android:name=".Activities.ProfileActivity" />
        <activity android:name=".Activities.LoginActivity" />
        <activity android:name=".Activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>